'use server'

// Schema validations

export async function exampleAction() {

    //#1. Preparing data with the schema to insert it into the database

    //#2. Inserting data into the database

    //#3. revalidatePath => function to clear cache of the patch (Next library)
    //#4. redirect => function to redirect to another page (Next library)

}